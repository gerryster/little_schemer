(load "deq?")

(define add1
  (lambda (n)
    (+ n 1)))

;(define plus
;  (lambda (a b)
;))

(write "add1")
(write (add1 0))
;(deq? '(2) ((add1 0)))
(write "done with the deq? check")


;(write "three element list")
;(write (firsts (quote ((a b) (c d) (e f)))))

